    <div class="comments">
        {{!-- we have to use this since arrays dont have named properties to use in this instance --}}
        {{#each this}}
        <section class="comment">
            <div class="meta">
                {{user.username}} on {{format_date created_at}}
            </div>
            <div class="text">
                {{comment_text}}
            </div>
        </section>
        {{/each}}
    </div>